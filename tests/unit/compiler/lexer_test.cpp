#include <gtest/gtest.h>
#include "core/compiler/lexer.h"

TEST(CompilerLexer, TokenizeBasic) {
    // Basic test
}

TEST(CompilerLexer, TokenizeQuantum) {
    // Quantum keyword test
}

TEST(CompilerLexer, TokenizeAI) {
    // AI keyword test
}
