module ui.component

type Component {
    id: string
    children: []Component
    props: map<string, Value>
    state: map<string, Value>
}

fn createElement(type: string, props: map<string, Value>) -> Component {
    // Component creation
}

fn render(component: Component) -> string {
    // Render implementation
}
