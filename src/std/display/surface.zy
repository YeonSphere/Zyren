type Surface {
    width: int
    height: int
    pixels: []byte
    stride: int
    format: PixelFormat
}

type PixelFormat {
    RGBA8
    BGRA8
    RGB565
}

fn create_surface(width: int, height: int, format: PixelFormat) -> Surface {
    // Creates drawing surface for rendering
}

fn blit(src: &Surface, dst: &Surface, src_rect: Rect, dst_rect: Rect) {
    // Fast surface copying with scaling support
}